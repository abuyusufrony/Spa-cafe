import PropTypes from 'prop-types';
import React from 'react';
import PropTypes from 'prop-types';


const Blog = ({ blg }) => {
    return (
        <div>

        </div>
    );
};
blg: PropTypes = {
    blg: PropTypes.object
}

export default Blog;